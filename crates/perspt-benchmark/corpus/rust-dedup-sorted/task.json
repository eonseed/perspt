{
  "goal": "implement dedup_sorted in src/lib.rs: remove consecutive duplicates from a sorted Vec<i64> in place, preserving order; handle the empty vector",
  "hidden_check": [
    "cargo",
    "test",
    "--quiet"
  ],
  "tags": [
    "small",
    "rust",
    "algorithm"
  ],
  "expect": "pass"
}
